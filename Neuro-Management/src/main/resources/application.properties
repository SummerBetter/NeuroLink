spring.application.name=Neuro-Management

# 数据库配置
spring.datasource.url=jdbc:mysql://localhost:3306/neurolink?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true&useUnicode=true&characterEncoding=utf8&autoReconnect=true&failOverReadOnly=false
spring.datasource.username=neuro
spring.datasource.password=Hx132919566@
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# 禁用数据库连接测试
spring.datasource.hikari.connection-test-query=SELECT 1
spring.datasource.hikari.validation-timeout=3000

# JPA配置 - 使用update模式，让Hibernate自动创建表
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect
spring.jpa.properties.hibernate.format_sql=true

# JWT配置
jwt.secret=NeuroLinkSecretKey2024ForJWTTokenGenerationAndValidation
jwt.expiration=86400000

# 服务器配置
server.port=8080
server.servlet.context-path=/api

# 日志配置
logging.level.com.hxun.neuromanagement=DEBUG
logging.level.org.springframework.security=DEBUG
